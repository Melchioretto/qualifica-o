\chapter{Proposta}
\label{cap:proposta}

% -----------------------------------------------------------------
% Objetivo do capítulo:
% Apresentar a proposta da dissertação. Começa pela caracterização
% metodológica da pesquisa, formaliza o problema e os objetivos com
% base no Capítulo 1 e na revisão sistemática do Capítulo 3, descreve
% as fontes de dados em nível operacional e segue para a arquitetura
% concreta da ferramenta. É aqui que entram diagramas de arquitetura,
% pseudocódigo dos indicadores, exemplos de saída, decisões de
% engenharia e ajustes feitos durante o desenvolvimento.
%
% Referência interna:
%   docs/pipeline/README.md
%   docs/pipeline/resumo_algoritmos.md
%   docs/pipeline/resumo_metodo_estatistico_global.txt
% -----------------------------------------------------------------


\section{Caracterização da pesquisa}
\label{sec:caracterizacao_pesquisa}

A pesquisa apresentada nesta dissertação tem natureza aplicada e caráter exploratório, orientada por problema concreto observado em ambiente industrial real. O trabalho não parte de uma hipótese formal a ser confirmada, mas sim de uma proposição central a ser construída e avaliada ao longo do desenvolvimento da ferramenta, conforme o problema enunciado na Seção~\ref{sec:problema_pesquisa}.

A análise opera exclusivamente sobre dados históricos coletados retrospectivamente da operação da fábrica. Aspectos de inferência em tempo real, geração de alertas operacionais e integração com sistemas de chão de fábrica para resposta imediata estão fora do escopo desta dissertação, embora constituam direção natural de trabalho futuro a ser indicada no capítulo final.

A escolha metodológica recai sobre técnicas não supervisionadas de análise de dados, justificada pela ausência de rótulo padronizado sobre a causa provável dos rompimentos, conforme discutido na Seção~\ref{sec:dados_disponiveis}. Em vez de tratar o evento de rompimento como um problema de classificação direta, a abordagem adotada combina dois eixos independentes de evidência. O primeiro analisa o comportamento operacional das máquinas a partir de séries temporais de variáveis sensoriais, enquanto o segundo analisa o histórico estatístico de rompimentos por lote, material, fornecedor e máquina. A integração entre os dois eixos produz o diagnóstico final da provável origem do evento.


\section{Problema de pesquisa}
\label{sec:problema_pesquisa}

O problema de pesquisa parte do quadro descrito no Capítulo~\ref{cap:fundamentacao}. O rompimento de fio é o sintoma operacional central da fábrica analisada, com impacto direto sobre disponibilidade da linha, retrabalho e perdas materiais. A investigação técnica desse sintoma esbarra em duas dificuldades concretas que justificam o tratamento dado nesta dissertação.

A primeira dificuldade é a sobreposição de mecanismos de causa. Conforme detalhado na Seção~\ref{sec:problema_rompimentos}, defeitos da matéria-prima, danos introduzidos na interação metal-máquina e combinações dos dois produzem fenótipos semelhantes na superfície de fratura, de modo que a inspeção direta após o evento raramente discrimina entre as hipóteses. O ponto observável do rompimento pode estar espacialmente desacoplado do ponto de origem do defeito, porque descontinuidades formadas na fundição e na laminação do vergalhão viajam embutidas no fio até serem expostas por solicitação mecânica posterior. Nesse cenário, atribuir a causa de cada evento exige cruzar informações que extrapolam o que está disponível no local da quebra.

A segunda dificuldade é a ausência de rótulo padronizado sobre a causa de cada rompimento nos dados operacionais. Conforme apresentado na Seção~\ref{sec:dados_disponiveis}, os eventos são registrados pelo sistema da fábrica, mas não vêm acompanhados de uma atribuição validada de origem provável, isto é, matéria-prima, equipamento ou combinação. Essa ausência impede o uso direto de abordagens supervisionadas que tratem o evento como problema de classificação. As fontes que poderiam servir como rótulo retrospectivo, como o relatório de devoluções de cliente, chegam meses após o evento e não cobrem o universo dos rompimentos internos, o que reforça a necessidade de uma estratégia que opere sobre evidências indiretas.

O problema de pesquisa desta dissertação é, portanto, o de combinar evidências de duas naturezas distintas, sinais operacionais coletados continuamente pelos sensores das máquinas e histórico estatístico de rompimentos segmentado por lote, material, fornecedor e máquina, para inferir a origem provável de cada evento em um cenário sem rótulo direto de causa. Dessa formulação decorre a pergunta orientadora desta pesquisa:
\begin{quote}
Como combinar sinais operacionais de máquina e evidências estatísticas históricas de rompimentos para inferir a provável origem de eventos de rompimento em uma fábrica de fios?
\end{quote}


\section{Objetivos}
\label{sec:objetivos}


\subsection{Objetivo geral}
\label{subsec:objetivo_geral}

Desenvolver uma ferramenta híbrida de apoio à decisão que combine sinais operacionais de máquina e evidências estatísticas históricas de rompimentos para inferir a provável origem de eventos de rompimento em uma fábrica de fios esmaltados de cobre e alumínio.


\subsection{Objetivos específicos}
\label{subsec:objetivos_especificos}

Para alcançar o objetivo geral, são definidos os seguintes objetivos específicos:
\begin{enumerate}
    \item Conduzir revisão sistemática da literatura sobre detecção de anomalias e análise estatística aplicadas à atribuição de causa em processos industriais com ausência de rótulos.
    \item Caracterizar e preparar as fontes de dados operacionais e históricos disponíveis para análise.
    \item Desenvolver o Pilar de Sinais Operacionais, responsável pela detecção de anomalias a partir de séries temporais de variáveis sensoriais.
    \item Desenvolver o Pilar de Histórico Estatístico, responsável pela análise de rompimentos segmentada por lote, material, fornecedor e máquina.
    \item Integrar os dois pilares em uma ferramenta de inferência e avaliar o resultado a partir de casos históricos da fábrica.
\end{enumerate}


\section{Fontes de dados}
\label{sec:fontes_dados}

% Esta secao descreve as duas tabelas brutas (status e sensores) que materializam os tres conjuntos de dados apresentados na Secao~\ref{sec:dados_disponiveis} do Cap. 2. As ordens de producao entram por cruzamento temporal com a tabela de status, conforme detalhado em 4.4.1.

Os três conjuntos de dados apresentados na Seção~\ref{sec:dados_disponiveis} se materializam na operação como duas tabelas brutas, cada uma com natureza e granularidade próprias. A tabela de status concentra o histórico cronológico de cada máquina, e nela os eventos de rompimento aparecem como categorias específicas de parada. A tabela de sensores reúne as leituras das variáveis monitoradas em formato longo. As ordens de produção, terceiro conjunto, não constituem tabela bruta autônoma e alimentam a tabela de status por cruzamento temporal, vinculando cada janela operacional ao lote, material e fornecedor processados. As Subseções~\ref{subsec:tabela_status} e~\ref{subsec:tabela_sensores} descrevem essas tabelas em detalhe, ilustradas nas Figuras~\ref{fig:tabelastatus} e~\ref{fig:tabelasensores}. A estrutura de cada tabela condiciona o tipo de técnica de análise aplicável a ela e justifica as transformações tratadas na Seção~\ref{sec:pipeline}.

\begin{figure}[htbp]
    \begin{minipage}[b]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Figuras/TabelaStatus.pdf}
        \caption{Representação visual da tabela de status.}
        \label{fig:tabelastatus}
        \fonte{Elaborado pelo autor (\the\year).}
    \end{minipage}\hfill
    \begin{minipage}[b]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Figuras/Sensores.pdf}
        \caption{Representação visual da tabela de sensores.}
        \label{fig:tabelasensores}
        \fonte{Elaborado pelo autor (\the\year).}
    \end{minipage}
\end{figure}


\subsection{Tabela de status}
\label{subsec:tabela_status}

A primeira tabela registra o histórico de status de cada máquina ao longo do tempo. Cada linha corresponde a um intervalo de operação contínua ou a um evento discreto que interrompeu essa operação. As colunas nativas trazem o identificador do programa associado à execução, o centro de máquina, o código e a descrição do evento, o instante inicial, o instante final e a duração do intervalo. A descrição do evento assume o valor \textsc{operando} nos intervalos de produção sem incidente, e valores específicos nos eventos de parada, incluindo categorias associadas a rompimentos por bolha, asperidade ou instabilidade de forno.

Atributos de produção como lote, material e fornecedor não fazem parte da tabela bruta. Eles são incorporados por cruzamento temporal com a tabela de ordens de produção, conforme detalhado na Seção~\ref{sec:pipeline}. A Figura~\ref{fig:tabelastatus} apresenta a tabela após esse cruzamento, com a granularidade visual preservada da tabela bruta. Cada linha representa um intervalo da máquina, e os eventos de rompimento aparecem como linhas pontuais dentro de um fluxo majoritariamente composto por intervalos de operação contínua.


\subsection{Tabela de sensores}
\label{subsec:tabela_sensores}

A segunda tabela é gerada pelo sistema de aquisição instalado nas máquinas e contém as leituras das variáveis monitoradas. Ela chega em formato longo, no qual cada linha registra uma leitura individual de um sensor, com identificação da variável de processo, do centro de máquina, do instante de registro e dos valores agregados máximo, médio e mínimo no intervalo amostral. O conjunto de variáveis cobre grandezas físicas e elétricas que descrevem o estado térmico, mecânico e elétrico da linha, com sensores distribuídos no recozedor, no forno, na zona de cura, nos pontos críticos do percurso do fio e nos elementos de controle térmico.

A Figura~\ref{fig:tabelasensores} apresenta a tabela. As operações necessárias para transformá-la no formato consumido pelos métodos de análise são descritas na Seção~\ref{sec:pipeline}.


\section{Visão geral da arquitetura}
\label{sec:arquitetura}

A ferramenta proposta nesta dissertação é denominada internamente WireSense. Seu projeto adota a estratégia anunciada na Seção~\ref{sec:caracterizacao_pesquisa}, que organiza a análise em dois pilares independentes de evidência, cada um operando sobre uma natureza distinta de dado e produzindo uma saída interpretável que descreve um aspecto do evento de rompimento. A inferência da provável origem decorre da combinação coordenada dessas duas saídas, formalizada nas regras de integração apresentadas na Seção~\ref{sec:regras_integracao}.

% TODO: criar diagrama de arquitetura em Figuras/wiresense_arquitetura.pdf e descomentar o bloco abaixo.
% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=0.85\textwidth]{Figuras/wiresense_arquitetura.pdf}
%     \caption{Visão geral da arquitetura do WireSense.}
%     \label{fig:wiresense_arquitetura}
%     \fonte{Elaborado pelo autor (\the\year).}
% \end{figure}

O Pilar de Sinais Operacionais opera sobre a tabela de sensores descrita na Seção~\ref{subsec:tabela_sensores}. Sua função é caracterizar o estado operacional da linha no entorno temporal de cada rompimento, comparando-o ao padrão esperado da operação. A saída do pilar é um indicador de anomalia por janela, que sinaliza se o comportamento das variáveis sensoriais antes do evento se afastou do regime nominal observado no histórico recente da máquina. A natureza da evidência produzida é local ao evento, atrelada à máquina específica em que o rompimento ocorreu, e independe da identidade do lote ou do fornecedor processados naquele momento.

O Pilar de Histórico Estatístico opera sobre a tabela de status descrita na Seção~\ref{subsec:tabela_status}, cruzada com as ordens de produção que vinculam cada janela operacional ao lote, material e fornecedor processados. Sua função é caracterizar o padrão histórico de rompimentos associado a cada combinação dessas dimensões, considerando a exposição produtiva acumulada por categoria. A saída do pilar é um indicador de risco relativo por lote, material, fornecedor e máquina, que sinaliza se a frequência de rompimentos observada em uma dada categoria se afasta da frequência esperada considerada a operação como um todo. A natureza da evidência produzida é agregada, refere-se a categorias de matéria-prima e configurações de máquina, e independe do comportamento sensorial imediato do evento sob análise.

A comunicação entre os dois pilares ocorre apenas no momento da inferência. Cada pilar produz sua saída de forma autônoma, sem trocar parâmetros internos ou compartilhar representações intermediárias. A integração opera sobre as duas saídas, combinadas segundo regras determinísticas que preservam a interpretabilidade do diagnóstico final. Essa decisão de projeto permite auditar cada pilar separadamente e atualizar um deles sem efeito sobre o outro, e é detalhada na Seção~\ref{sec:regras_integracao}.


\section{Pipeline de dados}
\label{sec:pipeline}

% Conteúdo a desenvolver:
%   - Sequência de etapas, das tabelas brutas ao diagnóstico.
%   - Mapa entre scripts implementados e etapas do pipeline.
%   - Pontos de integração com as bases existentes.


\section{Pilar de Sinais Operacionais}
\label{sec:impl_sinais}

% Conteúdo a desenvolver:
%   - Conjunto de 14 sensores e justificativa técnica da
%     escolha.
%   - Treinamento e ajuste do Isolation Forest.
%   - Critérios de SEM_MODELO e fallback ao Pilar de Histórico Estatístico.
%   - Exemplos de saída do pilar.


\section{Pilar de Histórico Estatístico}
\label{sec:impl_historico}

% Conteúdo a desenvolver:
%   - Indicadores calculados (taxas, exposição, risco relativo,
%     cruzamento entre máquinas).
%   - Decisão metodológica registrada em 12/05: o Pilar de Histórico
%     Estatístico conta apenas rompimentos, com bolha removida do
%     numerador.
%   - Regra MODERADA: o Pilar de Histórico Estatístico é acionável, o
%     ground truth do relatório de devoluções é reservado para
%     validação externa.
%   - Exemplos de saída do pilar.


\section{Regras de integração e diagnóstico final}
\label{sec:regras_integracao}

% Conteúdo a desenvolver:
%   - Lógica de combinação entre Pilar de Sinais Operacionais e Pilar de Histórico Estatístico.
%   - Categorias de origem provável.
%   - Forma de apresentação do diagnóstico (texto, indicadores,
%     evidência de apoio).


\section{Decisões de engenharia e ajustes iterativos}
\label{sec:decisoes_engenharia}

% Conteúdo a desenvolver:
%   - Principais decisões tomadas durante o desenvolvimento.
%   - Compromissos entre cobertura, precisão e
%     interpretabilidade.
%   - Pontos de extensibilidade da ferramenta.
